#include  

int val;
String duration;
String signal;

void setup() { 
  Serial.begin(9600); 
  pinMode(ldr, INPUT_PULLUP); 
   OUTPUT); 
  
  lcd.begin(16, 2); 
  lcd.clear(); 
  
  lcd.print(signal); 
  delay(200); 
  
